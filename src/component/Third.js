import React from 'react';
import Fourth from "./Fourth";

const Third = ({message}) => {
    return (
        <div>
            <Fourth message={message}/>
        </div>
    );
};

export default Third;